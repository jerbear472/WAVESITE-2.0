{
  "name": "wavesight-monorepo",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build:web": "cd web && npm install && npm run build",
    "start:web": "cd web && npm start",
    "dev:web": "cd web && npm run dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.52.1",
    "dotenv": "^17.2.1",
    "pg": "^8.16.3"
  }
}
